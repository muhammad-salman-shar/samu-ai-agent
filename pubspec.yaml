name: localagent
description: NovaAgent Local - Privacy-first Android AI Agent with native accessibility automation.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP client for local/remote AI backend communication
  http: ^1.2.0
  
  # State management
  provider: ^6.1.1
  
  # Persistent storage for settings
  shared_preferences: ^2.2.2
  
  # Animations
  flutter_animate: ^4.5.0
  
  # Cupertino style widgets
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/
